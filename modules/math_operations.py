#A Python file (.py) that contains code (functions, variables, classes) that can be reused in another file.
#Create a new Python file (e.g., math_operations.py) and write a few functions 


def subtract(a, b):
    return a - b

def divide(a, b):
    if b == 0:
        return "Cannot divide by zero"
    return a / b